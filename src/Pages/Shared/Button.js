import React from 'react';

const Button = () => {
    return (
        <div>
            <button class="banner-btn btn uppercase border-0 text-white font-bold">Get Started</button>
        </div>
    );
};

export default Button;